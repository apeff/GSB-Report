{% extends "layout.html.twig" %}
{% set VisitMenu = true %}

{% block title %}Rapport de visite{% endblock %}

{% block content %}
<h2 class="text-center">{{ block('title') }}</h2>
{% if drugs %}
    <div class="table-responsive">
        <table class="table table-hover table-condensed">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Pratitien</th>
                    <th>Ville</th>
                    <th>Motif</th>
                </tr>
            </thead>
            {% for drug in drugs %}
                <tr>
                    <td> {{ visitReports.date }}">{{ visitReports.practitioner.name }}</td>
                   
                </tr>
            {% endfor %}
        </table>
    </div>
{% else %}
    <div class="alert alert-warning">Aucun rapport trouvé.</div>
{% endif %}   
{% endblock %}




