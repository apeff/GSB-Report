{% extends "layout.html.twig" %}
{% set VisitMenu = true %}

{% block title %}Rapport de visite{% endblock %}

{% block content %}
<h2 class="text-center">{{ block('title') }}</h2>
{% if visitReports %}
    <div class="table-responsive">
        <table class="table table-hover table-condensed">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Pratitien</th>
                    <th>Ville</th>
                    <th>Motif</th>
                </tr>
            </thead>
            {% for visitReport in visitReports %}
                <tr>
                    <td> {{ visitReport.reportingDate }}">{{ visitReport.practitioner.name }}</td>
                   
                </tr>
            {% endfor %}
        </table>
    </div>
{% else %}
    <div class="alert alert-warning">Aucun rapport trouvé.</div>
{% endif %}   
{% endblock %}




